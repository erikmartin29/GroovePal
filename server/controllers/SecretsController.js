const dbConnection = require('../database/mySQLconnect');

const storeDiscogsApiKey = async (ctx) => {
    return new Promise((resolve, reject) => {
        const query = ``;
    }).catch( err => console.log(err));
};

const getDiscogsApiKey = async (ctx) => {
    return new Promise((resolve, reject) => {
        const query = ``;
    }).catch( err => console.log(err));
};

const storeLastfmApiKey = async (ctx) => {
    return new Promise((resolve, reject) => {
        const query = ``;
    }).catch( err => console.log(err));
};

const getLastfmApiKey = async (ctx) => {
    return new Promise((resolve, reject) => {
        const query = ``;
    }).catch( err => console.log(err));
};

module.exports = {
    storeDiscogsApiKey,
};
